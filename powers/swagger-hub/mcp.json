{
  "mcpServers": {
    "smartbear-mcp": {
      "command": "npx",
      "args": ["-y", "@smartbear/smartbear-mcp"],
      "env": {
        "SWAGGERHUB_API_KEY": "${SWAGGERHUB_API_KEY}"
      }
    }
  }
}
